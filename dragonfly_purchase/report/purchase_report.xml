<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="minimal_layout" inherit_id="web.minimal_layout">
        <xpath expr="//t[@t-if='subst_needed']" position="replace">
            <t t-if="subst_needed">
                <script>
            function subst() {
                var vars = {};
                var x = document.location.search.substring(1).split('&amp;');
                for (var i in x) {
                    var z = x[i].split('=', 2);
                    vars[z[0]] = unescape(z[1]);
                }
                var fromPage = document.getElementsByClassName('page');
                for(var j = 0; j&lt;fromPage.length; j++)
                    fromPage[j].textContent = vars['sitepage'];
                var toPage = document.getElementsByClassName('topage');
                for(var j = 0; j&lt;toPage.length; j++)
                    toPage[j].textContent = vars['sitepages'];

                var index = vars['webpage'].split('.', 4)[3]
                var header = document.getElementById('minimal_layout_report_headers');
                if(header !== null){
                    var companyHeader = header.children[index];
                    header.textContent = '';
                    header.appendChild(companyHeader);
                }
                var footer = document.getElementById('minimal_layout_report_footers');
                if(footer !== null){
                    var companyFooter = footer.children[index];
                    footer.textContent = '';
                    footer.appendChild(companyFooter);
                }
                var firstpage = document.getElementsByClassName('first-page')[0];
                if (vars['sitepage'] == 1) {
                    firstpage.style.visibility="visible";
                }else{
                    firstpage.style.visibility="hidden";
                }
            }
                </script>
            </t>
        </xpath>
    </template>
    <template id="report_external_layout_standard_dragonfly" inherit_id="web.external_layout_standard">
        <xpath expr="//div[hasclass('header')]//div[@name='company_address']" position="replace">
            <t t-if="o._name == 'purchase.order' ">
                <div class="col-6 first-page" name="company_address">
                    <div t-field="company.partner_id"
                        t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": true}'
                    />
                </div>
            </t>
            <t t-else="">
                <div class="col-6" name="company_address">
                    <div t-field="company.partner_id"
                        t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": true}'
                    />
                </div>
            </t>
        </xpath>
    </template>
</odoo>